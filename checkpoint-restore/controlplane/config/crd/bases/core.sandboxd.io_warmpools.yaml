---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: warmpools.core.sandboxd.io
spec:
  group: core.sandboxd.io
  names:
    kind: WarmPool
    listKind: WarmPoolList
    plural: warmpools
    shortNames:
    - wp
    singular: warmpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.idle
      name: Idle
      type: integer
    - jsonPath: .status.busy
      name: Busy
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WarmPool is the Schema for the warmpools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WarmPool
            properties:
              arbitraryImage:
                description: |-
                  arbitraryImage marks this pool as the landing zone for arbitrary-image
                  sessions (O6c): looser warm guarantees, fed by a generic base image.
                type: boolean
              minIdle:
                default: 1
                description: |-
                  minIdle keeps at least N idle workers ready to accept a session. (Enforcement
                  of the autoscaling to maintain minIdle is deferred to a later phase; the
                  field is honored as intent.)
                format: int32
                minimum: 0
                type: integer
              replicas:
                default: 1
                description: |-
                  replicas is the desired number of warm workers. HPA-scalable via the scale
                  subresource.
                format: int32
                minimum: 0
                type: integer
              templateRef:
                description: templateRef names the SandboxTemplate this pool's workers
                  serve.
                properties:
                  name:
                    description: name of the referenced object.
                    type: string
                required:
                - name
                type: object
            required:
            - replicas
            - templateRef
            type: object
          status:
            description: status defines the observed state of WarmPool
            properties:
              busy:
                description: busy is the number of workers currently holding a session.
                format: int32
                type: integer
              conditions:
                description: conditions represent the current state of the WarmPool
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              idle:
                description: idle is the number of workers currently holding no session.
                format: int32
                type: integer
              replicas:
                description: replicas is the number of worker pods currently owned
                  by this pool.
                format: int32
                type: integer
              selector:
                description: selector is the label selector for the scale subresource
                  / HPA.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.selector
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.replicas
      status: {}
