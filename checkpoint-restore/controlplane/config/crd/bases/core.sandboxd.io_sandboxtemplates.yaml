---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: sandboxtemplates.core.sandboxd.io
spec:
  group: core.sandboxd.io
  names:
    kind: SandboxTemplate
    listKind: SandboxTemplateList
    plural: sandboxtemplates
    shortNames:
    - sbxt
    singular: sandboxtemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SandboxTemplate is the Schema for the sandboxtemplates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SandboxTemplate
            properties:
              cmd:
                description: cmd overrides the image entrypoint+cmd (optional; image
                  default otherwise).
                items:
                  type: string
                type: array
              env:
                description: env is added to the sandbox process environment.
                items:
                  type: string
                type: array
              health:
                description: health drives the worker readiness probe (and thus router
                  health/idle).
                properties:
                  probe:
                    description: probe type used to determine readiness.
                    enum:
                    - none
                    - tcp
                    - http
                    type: string
                  probePath:
                    description: probePath is the HTTP path to probe (http probe only).
                    type: string
                  probePort:
                    description: probePort is the interior container port to probe.
                    type: integer
                  restartPolicy:
                    description: restartPolicy on unexpected sandbox exit.
                    enum:
                    - none
                    - cold
                    - restore
                    type: string
                type: object
              idle:
                description: idle controls how long a sandbox may be idle before checkpoint
                  + reclaim.
                properties:
                  action:
                    default: suspend
                    description: |-
                      action taken when idle: suspend (checkpoint->S3, free worker),
                      reset (discard state, free worker), or none.
                    enum:
                    - suspend
                    - reset
                    - none
                    type: string
                  timeoutSeconds:
                    default: 300
                    description: timeoutSeconds of inactivity before the action fires.
                      0 = never auto-suspend.
                    minimum: 0
                    type: integer
                type: object
              image:
                description: |-
                  image is the OCI image run AS THE SANDBOX (nested gVisor workload), not the
                  worker pod image.
                type: string
              ports:
                description: ports are exposed via the worker's DNAT (podIP:host ->
                  interiorIP:container).
                items:
                  description: PortMap maps a worker pod port to a sandbox container
                    port (DNAT).
                  properties:
                    container:
                      description: container is the port the sandbox process listens
                        on.
                      maximum: 65535
                      minimum: 1
                      type: integer
                    host:
                      description: host is the port on the worker pod IP. 0 defaults
                        to container.
                      maximum: 65535
                      minimum: 0
                      type: integer
                  required:
                  - container
                  type: object
                type: array
              resources:
                description: resources is a worker sizing hint informing the WarmPool
                  pod resources.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              scheduling:
                description: |-
                  scheduling controls worker-pod placement (nodeSelector/tolerations/spread).
                  Optional; the operator applies gVisor-node defaults when unset.
                properties:
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: |-
                      nodeSelector constrains which nodes workers land on. Defaults to
                      {sandbox: gvisor} when nil.
                    type: object
                  spreadAcrossNodes:
                    description: |-
                      spreadAcrossNodes enables soft pod anti-affinity so a pool's workers prefer
                      distinct nodes (survives a node loss; gives cross-node teleport a target).
                      Defaults to true when nil.
                    type: boolean
                  tolerations:
                    description: |-
                      tolerations applied to worker pods. Defaults to tolerating the
                      sandbox=gvisor:NoSchedule taint when nil.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                            Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
            required:
            - image
            type: object
          status:
            description: status defines the observed state of SandboxTemplate
            properties:
              conditions:
                description: conditions represent the current state of the SandboxTemplate
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
